<%= Rouge::Themes::Monokai.render(:scope => '.highlight') %>

$pre-bg: #49483e;

pre {
  background-color: $pre-bg;
  border-color: lighten($pre-bg, 50%);
  overflow-x: auto;
  code {
    word-wrap: normal;
    white-space: pre;
  }
}
